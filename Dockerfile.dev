# Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy package manifests first and install dependencies
COPY package*.json ./
RUN npm ci --production

# Copy app source
COPY . .

ENV NODE_ENV=production
EXPOSE 3000

CMD ["node", "index.js"]
